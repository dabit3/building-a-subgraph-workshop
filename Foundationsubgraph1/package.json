{
  "name": "fnddev",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ dabit3/foundationsubgraph",
    "create-local": "graph create --node http://localhost:8020/ dabit3/foundationsubgraph",
    "remove-local": "graph remove --node http://localhost:8020/ dabit3/foundationsubgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dabit3/foundationsubgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.24.0",
    "@graphprotocol/graph-ts": "0.24.0"
  }
}
